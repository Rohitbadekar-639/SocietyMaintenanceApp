# Production — set in Vercel → Project → Settings → Environment Variables
# Use your Render public URLs (no trailing slash after /api/v1)

VITE_IDENTITY_URL=https://societywale-identity.onrender.com/api/v1
VITE_CORE_URL=https://societywale-core.onrender.com/api/v1
